# ── Gradle / Android build output ────────────────────────────────────
.gradle/
build/
android/build/
android/app/build/
android/captures/
local.properties
*.apk
*.aab
*.aar
*.ap_
*.dex
*.class
# The wrapper JAR is intentionally committed (that is the point of a wrapper).
!gradle/wrapper/gradle-wrapper.jar

# ── IDE ──────────────────────────────────────────────────────────────
.idea/
*.iml
*.iws
*.ipr
.kotlin/
.cxx/

# ── Parity harness working files (regenerated by tools/parity/run.sh) ─
tools/parity/node_modules/
tools/parity/work/

# ── OS noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
